apply plugin: 'maven'
apply plugin: 'signing'
apply plugin: 'application'

mainClassName = "com.comsysto.buildlight.application.commandline.Runner"

group = 'com.comsysto.buildlight'

sourceSets {
    main {
        resources {
            srcDir 'src/main/java'
            srcDir 'src/main/resources'
        }
    }
    test {
        resources {
            srcDir 'src/test/java'
            srcDir 'src/test/resources'
        }
    }
}

dependencies {
    compile project(":driver:cleware-driver")
    compile project(":driver:arduino-driver")

    compile project(":repository:bamboo:api")
    compile project(":repository:bamboo:model")
    compile project(":repository:bamboo:impl")

    compile project(":repository:jenkins:api")
    compile project(":repository:jenkins:model")
    compile project(":repository:jenkins:impl")

    compile commonDeps
    compile springDeps

    compile "org.springframework:spring-web:$springVersion"
    compile "org.apache.httpcomponents:httpclient:$httpClientVersion"

    compile files("${System.properties['java.home']}/lib/jfxrt.jar")

    testCompile testDeps
}

jar {
    manifest {
        attributes(
                "Implementation-Title": "Build Light Application",
                "Implementation-Version": version,
                "Author": "Bernd Zuther <bernd.zuther@comsysto.com>",
                "Main-Class": mainClassName
        )
    }
}

run {
    if (project.hasProperty('args')) {
        args project.args.split('\\s+')
    }
}

/*
artifacts {
    archives jar
    archives javadocJar
    archives sourcesJar
    archives distZip
    archives distTar
}

signing {
    sign configurations.archives
}

uploadArchives {
    repositories {
        mavenDeployer {
            beforeDeployment { MavenDeployment deployment -> signing.signPom(deployment) }

            repository(url: "https://oss.sonatype.org/service/local/staging/deploy/maven2/") {
                authentication(userName: sonatypeUsername, password: sonatypePassword)
            }

            pom.project {
                name 'Build Light Application'
                packaging 'zip'
                description 'Build Light Light Application'
                url 'https://github.com/comsysto/build-light'

                scm {
                    url 'scm:git@github.com:comsysto/build-light.git'
                    connection 'scm:git@github.com:comsysto/build-light.git'
                    developerConnection 'scm:git@github.com:comsysto/build-light.git'
                }

                licenses {
                    license {
                        name 'The Apache Software License, Version 2.0'
                        url 'http://www.apache.org/licenses/LICENSE-2.0.txt'
                        distribution 'repo'
                    }
                }

                developers {
                    developer {
                        id 'zutherb'
                        name 'Bernd Zuther'
                    }
                }
            }
        }
    }
}
*/